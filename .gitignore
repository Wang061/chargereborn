# ===== ESP-IDF 构建产物（实际产物在 hello_world/build/，用 **/ 全覆盖）=====
**/build/
**/config.env
sdkconfig
sdkconfig.old
*.elf
*.bin
*.map
# *.espdl 默认是构建产物(build/espdl_models/ 已被上面的 **/build/ 忽略);
# 但 vendored 进 components/ 的自训模型是参赛交付物, 必须入库
*.espdl
!components/battery_detect/models/**/*.espdl
managed_components/
datasheets/

# ===== 本地开发环境 / 私有内容（不入库）=====
.claude/
.mcp.json
_research/
计划.docx

# ===== harness 运行时 =====
logs/
.venv-tools/
.venv/

# ===== IDE / 工具缓存（clangd 索引等，勿入库）=====
.cache/
.serena/cache/
compile_commands.json

# ===== Python =====
__pycache__/
*.pyc
*.pyo

# ===== 编辑器 / 系统 / Office 锁文件 =====
~$*
*.tmp
Thumbs.db
.DS_Store

# 说明：以下"应当提交"，故不忽略——
#   sdkconfig.defaults / sdkconfig.defaults.esp32s3 / sdkconfig.ci（构建配置源）
#   dependencies.lock（组件版本锁，保可复现）
#   .claude/settings.json（项目级 harness 配置）
#   docs/、_research/、计划.docx（设计与调研记录）
